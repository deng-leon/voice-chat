# Camunda 8 Zeebe Configuration (Local docker-compose-full with Keycloak)
ZEEBE_ADDRESS=localhost:26500

# Authentication for local Keycloak (docker-compose-full)
# IMPORTANT: The token 'iss' (Issuer) must match Zeebe's ZEEBE_GATEWAY_SECURITY_AUTHENTICATION_IDENTITY_ISSUERBACKENDURL exactly!
# If Zeebe uses 'http://keycloak:8080/...' inside Docker, you may need to map 'keycloak' to 127.0.0.1 in your /etc/hosts
# and use 'http://keycloak:18080/...' here instead.
ZEEBE_CLIENT_ID=orchestration
ZEEBE_CLIENT_SECRET=secret
ZEEBE_AUTHORIZATION_SERVER_URL=http://localhost:18080/auth/realms/camunda-platform/protocol/openid-connect/token
ZEEBE_TOKEN_AUDIENCE=zeebe-api

# Voice AI Configuration
# This system prompt is passed to the frontend LLM.
VOICE_AI_SYSTEM_PROMPT="You are a helpful voice assistant of Camunshopia, an e-commerce store selling vacuums and routers. Be warm and friendly."
